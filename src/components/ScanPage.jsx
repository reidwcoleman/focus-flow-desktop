import Scanner from './Scanner'

const ScanPage = () => {
  return (
    <Scanner
      onClose={() => {}}
      onCapture={() => {}}
      initialScanMode="homework"
    />
  )
}

export default ScanPage
